<div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4">
  <% @project.images.each do |image| %>
    <%= render partial: 'partials/image-thumbnail', locals: {image: image} %>
  <% end %>
</div>

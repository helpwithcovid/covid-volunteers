<select name="sort_by" id="sort_by" class="w-full sm:w-auto text-sm text-gray-700 bg-white form-select shadow-sm text-gray-500 sm:text-sm" @input="window.location.href = event.target.value">
  <%= yield %>
</select>
